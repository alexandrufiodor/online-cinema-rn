import { FC } from 'react'
import Actor from '@/components/screens/actor/Actor'

const ActorSlug: FC = () => {
	// const { slug } = useLocalSearchParams<{ slug: string }>()
	return <Actor />
}

export default ActorSlug
